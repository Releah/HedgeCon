export default function ConfirmDialog({ eyebrow = 'PLEASE CONFIRM', title, message, confirmLabel, danger = false, busy = false, onConfirm, onCancel }: { eyebrow?: string; title: string; message: string; confirmLabel: string; danger?: boolean; busy?: boolean; onConfirm: () => void | Promise<void>; onCancel: () => void }) {
  return <div className="overlay confirm-overlay" role="presentation" onMouseDown={event => { if (event.target === event.currentTarget && !busy) onCancel(); }}><section className="dialog confirm-dialog" role="alertdialog" aria-modal="true" aria-labelledby="confirm-title" aria-describedby="confirm-message"><div className={`confirm-symbol ${danger ? 'danger' : ''}`}>{danger ? '!' : '?'}</div><small>{eyebrow}</small><h2 id="confirm-title">{title}</h2><p id="confirm-message">{message}</p><div className="actions"><button className="secondary" disabled={busy} onClick={onCancel}>Cancel</button><button autoFocus className={danger ? 'danger-button' : 'primary'} disabled={busy} onClick={() => void onConfirm()}>{busy ? 'Working…' : confirmLabel}</button></div></section></div>;
}
